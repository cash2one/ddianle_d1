#include "HandbookStruct.h"

CHandbookUnit::CHandbookUnit(int id, int PraiseCount, int EnvyCount ,int CollectCount)
{
    SetID(id);
    SetPraisedCount(PraiseCount);
    SetEnvyCount(EnvyCount);
    SetCollectCount(CollectCount);
}

CHandbookPersonalInfo::CHandbookPersonalInfo(int id, int Praised, int Envy, int Collect)
{
    SetID(id);
    SetPraised(Praised);
    SetEnvyed(Envy);
    SetCollected(Collect);
}
